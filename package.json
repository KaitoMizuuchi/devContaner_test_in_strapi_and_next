{
  "name": "dccorp-root",
  "private": true,
  "scripts": {
    "setup": "cd backend && npm install && cd ../frontend && npm install",
    "start-strapi": "cd backend && npm run develop",
    "start-next": "cd frontend && npm run dev",
    "start-all": "npm-run-all -p start-strapi start-next"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
